/**
 *
 * @param a
 * @param b
 * @returns {*}
 */

function sum(a, b) {
    return a + b;
}

/**
 *
 * @param param
 * @returns {number}
 */
function count(param){
    let result = 1;
    for (let i = 1; i <= a; i++) {
        result = result * i;
    }
    return result;
}
